import { Route } from '@angular/router';
import {AuthSetPasswordComponent} from './set-password.component';

export const authSetPasswordRoutes: Route[] = [
    {
        path     : '',
        component: AuthSetPasswordComponent
    }
];
